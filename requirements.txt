# ---------------------------------------------------------------------------
# FastFusion-IF Python dependencies
#
# PyTorch is intentionally NOT pinned in this file, because the correct build
# depends on your GPU / CUDA driver. Install it FIRST (see README, "Installation").
# Reference environment used for the paper:
#     Python 3.10, torch==2.4.1 with CUDA 12.1
#     conda install pytorch==2.4.1 torchvision==0.19.1 torchaudio==2.4.1 \
#                   pytorch-cuda=12.1 -c pytorch -c nvidia
# ---------------------------------------------------------------------------

numpy>=1.24
scipy>=1.10
pandas>=2.0
scikit-learn>=1.3
biopython>=1.81
tqdm>=4.66
dill>=0.3.7
fair-esm>=2.0.0          # ESM-2 residue embeddings (used by the benchmark and DIPS pipelines)

# Optional, ONLY needed to regenerate the leakage-safe DIPS-Plus splits
# (not required for the AGAT-PPIS benchmark). Install via conda:
#     conda install -c bioconda -c conda-forge mmseqs2
